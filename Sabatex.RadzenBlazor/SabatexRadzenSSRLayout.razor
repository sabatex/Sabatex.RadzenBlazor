@inject SabatexJsInterop SabatexJsInterop

@code {
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        if (OperatingSystem.IsBrowser())
            await SabatexJsInterop.RadzenBlazorSSRLayout();
    }
}
